spring:
  config:
    activate:
      on-profile: prod
  profiles:
    active: prod

  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:housing-finance-mortgage}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 15
      connection-timeout: 5000
      idle-timeout: 30000
      max-lifetime: 60000

  kafka:
    bootstrap-servers: ${KAFKA_HOST:kafka}:${KAFKA_PORT:9092}
    consumer:
      group-id: mortgage-group-prod
      auto-offset-reset: latest

aws:
  s3:
    bucket-name: ${AWS_S3_BUCKET:mortgage-documents}
    region: ${AWS_REGION:us-east-1}
    endpoint: ${AWS_S3_ENDPOINT:}
    access-key: ${AWS_ACCESS_KEY:}
    secret-key: ${AWS_SECRET_KEY:}

opentelemetry:
  exporter:
    otlp:
      endpoint: ${OTLP_ENDPOINT:http://otel-collector:4317}

logging:
  level:
    root: INFO
    org.springframework.web: WARN
    com.okoth.mortgage: INFO
  file:
    name: /var/log/mortgage-portal/mortgage-portal.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"