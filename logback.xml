<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}/spring.log}"/>
    <property name="LOG_LEVEL" value="${LOG_LEVEL:-INFO}"/>
    <property name="JSON_LOG_PATTERN"
              value='{
                "time":"%date{ISO8601}",
                "level":"%level",
                "service":"${spring.application.name:-unknown}",
                "traceId":"%mdc{traceId:-}",
                "spanId":"%mdc{spanId:-}",
                "thread":"%thread",
                "logger":"%logger{40}",
                "message":"%message",
                "exception":"%exception{10}",
                "stacktrace":"%ex{10}",
                "context":"%mdc"
              }'/>

    <appender name="consoleJson" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                    <fieldName>time</fieldName>
                </timestamp>
                <logLevel>
                    <fieldName>level</fieldName>
                </logLevel>
                <loggerName>
                    <fieldName>logger</fieldName>
                </loggerName>
                <threadName>
                    <fieldName>thread</fieldName>
                </threadName>
                <message>
                    <fieldName>message</fieldName>
                </message>
                <throwableProxy>
                    <fieldName>stacktrace</fieldName>
                </throwableProxy>
                <mdc>
                    <fieldName>context</fieldName>
                </mdc>
                <arguments>
                    <fieldName>args</fieldName>
                </arguments>
                <context/>
                <pattern>
                    <pattern>
                        {
                        "service":"${spring.application.name:-unknown}",
                        "version":"${spring.application.version:-unknown}",
                        "env":"${spring.profiles.active:-default}"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>

    <appender name="fileJson" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${spring.application.name:-unknown}","env":"${spring.profiles.active:-default}"}</customFields>
            <includeContext>false</includeContext>
            <timeZone>UTC</timeZone>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <springProfile name="dev,local">
        <appender name="consolePretty" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%15.15thread] %cyan(%-40.40logger{40}) : %msg%n%ex{10}</pattern>
            </encoder>
        </appender>
    </springProfile>

    <root level="${LOG_LEVEL}">
        <appender-ref ref="consoleJson"/>
        <appender-ref ref="fileJson"/>
        <springProfile name="dev,local">
            <appender-ref ref="consolePretty"/>
        </springProfile>
    </root>

    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="INFO"/>
    <logger name="org.apache.kafka" level="WARN"/>

    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>
</configuration>